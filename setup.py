from distutils.core import setup
import setuptools

setup(name='shmgpsd',
      version='0.2.4',
      description='GPSD shared memory tool',
      package_dir={'':'shmgpsd'},
      py_modules=['shmgpsd'],
      )
